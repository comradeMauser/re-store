import React from 'react';

const About = () => {
    return (
        <div className="container">
            <span className="about">...we have been working at work for a long time, only to work even more at two jobs later, and all this,
                for you...</span>
        </div>
    );
};

export default About;